import React from 'react'
import { View, Text } from 'react-native'

const StyledButton = (props) => {
    return (
        <View>
            <Text>Syled Button Component</Text>
        </View>
    )
}

export default StyledButton
